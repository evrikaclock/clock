# Evrika clock
## Requirements

python 3.6+

## Installation

on Windows:
```
>python -m venv venv
>.\venv\Scripts\activate.bat
(venv)>pip install -r requirements.txt
```

## Usage
```(venv)>python main.py```
